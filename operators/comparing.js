let one = 1
let two = 2

// equal
console.log(one == 1)
console.log(one == '1')
console.log(one === '1')
console.log(one == 2)

// Different
console.log(one != two)
console.log (two != two)

// Strictly equal - compares type and value
console.log(one === '1')
console.log(one === 1)

// Strictly different - compares type and value
console.log(one !== 1)
console.log(one !== '1')

// Bigger/Samller than
console.log( one > two )
console.log( one < two )
